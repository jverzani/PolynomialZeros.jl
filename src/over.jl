module Over
# use to specify domain

# immutable -> struct
immutable CC{T} end
immutable C end

immutable RR{T} end
immutable R end

immutable QQ{T} end
immutable Q end

immutable ZZ{T} end
immutable Z end

immutable Zp{p} end

end
